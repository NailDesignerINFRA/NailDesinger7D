# ============================================================
# Cloudflare Pages — config do projeto Nail Designer 7D
# Deploy é feito automaticamente via GitHub.
# Este arquivo permite usar `wrangler pages` localmente também.
# ============================================================

name = "naildesigner7d"
compatibility_date = "2025-05-23"
pages_build_output_dir = "."

# Não há build step — site estático (HTML/CSS/JS puro).
# O Cloudflare Pages serve direto os arquivos do root.
